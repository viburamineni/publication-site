---
interface Props {
  title?: string;
  message?: string;
}

const {
  title = 'The next edition is in progress',
  message = 'New reporting will appear here as it is published.',
} = Astro.props;
---

<section class="empty-state">
  <h2>{title}</h2>
  <p>{message}</p>
</section>

---
interface Props {
  title?: string;
  message?: string;
}

const { title = 'More stories are on the way', message = 'Check back soon for new reporting.' } =
  Astro.props;
---

<section class="empty-state">
  <h2>{title}</h2>
  <p>{message}</p>
</section>
